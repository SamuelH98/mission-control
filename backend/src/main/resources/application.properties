spring.application.name=mission-control-backend

app.db.path=${APP_DB_PATH:data/mission-control.db}

server.port=${SERVER_PORT:8080}

spring.sql.init.mode=always

logging.level.org.springframework.jdbc=INFO
